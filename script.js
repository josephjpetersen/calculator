function getSum (x, y) {
  return x + y;
}

function getDifference (x, y) {
  return x - y;
}

function getProduct (x, y) {
  return x * y;
}

function getQuotient (x, y) {
  return x / y;
}

function getRemainder (x, y) {
  return x % y;
}